import PageHeader from '../../components/PageHeader/PageHeader'

const DBBackpupPage = () => {
  return (
    <PageHeader
      title="DB"
      description="Manage GitHub configs, users, and repositories per company."
    />
  )
}

export default DBBackpupPage
